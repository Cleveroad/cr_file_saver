name: cr_file_saver_example
description: Demonstrates how to use the cr_file_saver plugin.

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cr_file_saver:
    path: ../

  cupertino_icons: ^1.0.2
  path_provider:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true
